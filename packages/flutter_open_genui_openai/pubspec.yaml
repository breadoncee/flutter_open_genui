name: flutter_open_genui_openai
description: >-
  OpenAI (and OpenAI-compatible) adapter for flutter_open_genui — drive Flutter
  GenUI A2UI surfaces with the OpenAI Chat Completions API.
version: 0.0.1
repository: https://github.com/breadoncee/flutter_open_genui
issue_tracker: https://github.com/breadoncee/flutter_open_genui/issues

topics:
  - ai
  - llm
  - openai
  - genui
  - generative-ui

environment:
  sdk: ^3.10.0
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_open_genui: ^0.0.1
  genui: ^0.9.0
  dio: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
