## 0.0.1

- Initial release. `GenUiAdapter` base class, `GenUiAdapterConfig`,
  `GenUiRequest`, prompt/logging helpers, and the `JsonRepair` /
  `A2uiJsonHealer` resilient streaming layer. Built against `genui` 0.9.0.
