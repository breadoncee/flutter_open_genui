name: example
description: Example app for flutter_open_genui — drive Flutter GenUI with OpenAI.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.9.0
  flutter_open_genui: ^0.0.1
  flutter_open_genui_openai: ^0.0.1
  flutter_open_genui_openrouter: ^0.0.1
  flutter_open_genui_anthropic: ^0.0.1
  flutter_open_genui_ollama: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  dio: ^5.0.0
